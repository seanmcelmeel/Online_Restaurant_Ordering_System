const orderItems = require("../models/order.items.model.js");

// Create and Save a new order items
exports.create = (req, res) => {
  
};

// Retrieve all order items from the database (with condition).
exports.findAll = (req, res) => {
  
};

// Find a single order items with a id
exports.findOne = (req, res) => {
  
};

// find all published order items
exports.findAllPublished = (req, res) => {
  
};

// Update a order items identified by the id in the request
exports.update = (req, res) => {
  
};

// Delete a order items with the specified id in the request
exports.delete = (req, res) => {
  
};

// Delete all order items from the database.
exports.deleteAll = (req, res) => {
  
};